{
  "kind": "collectionType",
  "collectionName": "canonical_rules",
  "info": {
    "singularName": "canonical-rule",
    "pluralName": "canonical-rules",
    "displayName": "SEO Manager - Canonical Rules",
    "description": "Optional canonical URL overrides for normalized public paths."
  },
  "options": {"draftAndPublish": true},
  "pluginOptions": { "content-manager": { "visible": false } },
  "attributes": {
    "source_path": {"type": "string", "required": true, "unique": true, "maxLength": 2048},
    "canonical_url": {"type": "string", "required": true, "maxLength": 2048},
    "is_active": {"type": "boolean", "default": true},
    "notes": {"type": "text", "maxLength": 1000}
  }
}
